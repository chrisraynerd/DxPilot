<UserControl x:Class="JtdxAutoResume.V3.Views.SessionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="0,12,0,0">
        <Border DockPanel.Dock="Top" Style="{StaticResource PanelBorder}" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Export CSV" Command="{Binding SessionHistory.ExportCommand}" />
                    <Button Content="Clear" Command="{Binding SessionHistory.ClearCommand}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Session History" FontWeight="SemiBold" FontSize="16" />
                    <TextBlock Text="{Binding SessionHistory.Status}" Foreground="#526577" Margin="0,2,0,8" />
                    <Border BorderBrush="#D8E0E8" BorderThickness="0,0,0,1" Padding="0,0,0,8" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.4*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2.2*" />
                                <ColumnDefinition Width="1.6*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding CurrentTargetStatus.OperatingMode}" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="{Binding CurrentTargetStatus.SelectedTargetDisplay}" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="2" Text="{Binding CurrentTargetStatus.PlainStatusMessage}" TextWrapping="Wrap" />
                            <TextBlock Grid.Column="3" Text="{Binding CurrentTargetStatus.TxGateStatus}" TextWrapping="Wrap" />
                        </Grid>
                    </Border>
                    <TextBlock Text="Wanted type" FontWeight="SemiBold" />
                    <WrapPanel>
                        <CheckBox Content="DXCC" IsChecked="{Binding SessionHistory.ShowDxcc}" />
                        <CheckBox Content="Grids" IsChecked="{Binding SessionHistory.ShowGrids}" />
                        <CheckBox Content="USA States" IsChecked="{Binding SessionHistory.ShowUsaStates}" />
                        <CheckBox Content="Rare confirmed DXCC" IsChecked="{Binding SessionHistory.ShowRareConfirmed}" />
                    </WrapPanel>
                    <TextBlock Text="Outcome" FontWeight="SemiBold" Margin="0,6,0,0" />
                    <WrapPanel>
                        <CheckBox Content="Seen only" IsChecked="{Binding SessionHistory.ShowSeenOnly}" />
                        <CheckBox Content="Called" IsChecked="{Binding SessionHistory.ShowCalledOnly}" />
                        <CheckBox Content="Worked/logged" IsChecked="{Binding SessionHistory.ShowWorked}" />
                        <CheckBox Content="Missed" IsChecked="{Binding SessionHistory.ShowMissed}" />
                        <CheckBox Content="Suppressed" IsChecked="{Binding SessionHistory.ShowSuppressed}" />
                        <CheckBox Content="Failed / mismatch" IsChecked="{Binding SessionHistory.ShowFailedMismatch}" />
                    </WrapPanel>
                </StackPanel>
            </DockPanel>
        </Border>

        <TextBox DockPanel.Dock="Bottom"
                 Text="{Binding SessionHistory.SelectedOpportunity.Details, Mode=OneWay, TargetNullValue=Select a DX opportunity to see details.}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MinHeight="150"
                 MaxHeight="230"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,10,0,0" />

        <Border DockPanel.Dock="Bottom" Style="{StaticResource PanelBorder}" Margin="0,10,0,0" MaxHeight="110">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Session History Recent Actions" FontWeight="SemiBold" Margin="0,0,0,4" />
                <ListBox ItemsSource="{Binding SessionHistoryRecentActions}" BorderThickness="0" />
            </DockPanel>
        </Border>

        <DataGrid ItemsSource="{Binding SessionHistory.Opportunities}"
                  SelectedItem="{Binding SessionHistory.SelectedOpportunity}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserSortColumns="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding OutcomeClass}" Value="Worked">
                            <Setter Property="Background" Value="#CFF7D3" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OutcomeClass}" Value="Missed">
                            <Setter Property="Background" Value="#FFD6D6" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OutcomeClass}" Value="Suppressed">
                            <Setter Property="Background" Value="#FFE8A3" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OutcomeClass}" Value="InProgress">
                            <Setter Property="Background" Value="#CFE8FF" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OutcomeClass}" Value="WantedDxcc">
                            <Setter Property="Background" Value="#E8D5FF" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OutcomeClass}" Value="Rare">
                            <Setter Property="Background" Value="#F4F8FF" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Last Seen" Binding="{Binding LastSeenText}" Width="85" />
                <DataGridTextColumn Header="Seen For" Binding="{Binding SeenForText}" Width="80" />
                <DataGridTextColumn Header="Ago" Binding="{Binding AgeText, Mode=OneWay}" Width="70" />
                <DataGridTextColumn Header="Call" Binding="{Binding Call}" Width="95" />
                <DataGridTextColumn Header="Country" Binding="{Binding Entity}" Width="145" />
                <DataGridTextColumn Header="Wanted Reason" Binding="{Binding PrimaryReason}" Width="260" />
                <DataGridTextColumn Header="Need" Binding="{Binding Need}" Width="85" />
                <DataGridTextColumn Header="Grid / State" Binding="{Binding GridStateText}" Width="85" />
                <DataGridTextColumn Header="Best SNR" Binding="{Binding BestSnrText}" Width="75" />
                <DataGridTextColumn Header="Times Seen" Binding="{Binding TimesSeenText}" Width="85" />
                <DataGridTextColumn Header="Called?" Binding="{Binding CalledText}" Width="70" />
                <DataGridTextColumn Header="Worked/logged?" Binding="{Binding WorkedText}" Width="105" />
                <DataGridTextColumn Header="Outcome" Binding="{Binding Outcome}" Width="135" />
                <DataGridTextColumn Header="Outcome Reason" Binding="{Binding OutcomeReason}" Width="210" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
