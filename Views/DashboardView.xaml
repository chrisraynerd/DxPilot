<UserControl x:Class="JtdxAutoResume.V3.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Border Grid.ColumnSpan="2" Style="{StaticResource PanelBorder}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="LIVE MONITOR" Style="{StaticResource SectionEyebrow}" />
                    <TextBlock Text="Decodes and DX Pilot activity" Style="{StaticResource WorkspaceTitle}" Margin="0,2,0,0" />
                    <TextBlock Text="A live evidence view of what JTDX is hearing and the decisions DX Pilot is making."
                               Foreground="{StaticResource AppTextMuted}" Margin="0,3,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Dashboard.OverallStatus}" FontWeight="SemiBold" TextAlignment="Right" TextWrapping="Wrap" MaxWidth="560" />
                    <TextBlock Text="{Binding Dashboard.UdpStatus}" Foreground="{StaticResource AppTextMuted}" TextAlignment="Right" TextWrapping="Wrap" MaxWidth="560" Margin="0,3,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource PanelBorder}" Margin="0,0,12,0">
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="DX PILOT ACTIVITY" Style="{StaticResource SectionEyebrow}" />
                        <TextBlock Text="Recent decisions and actions" FontSize="17" FontWeight="SemiBold" Margin="0,2,0,0" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="Export" Command="{Binding ExportRecentActionsCommand}" MinWidth="74" Margin="0" />
                </Grid>
                <ListBox ItemsSource="{Binding RecentActions}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto" />
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource PanelBorder}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                    <TextBlock Text="JTDX INPUT" Style="{StaticResource SectionEyebrow}" />
                    <TextBlock Text="Live decode feed" FontSize="17" FontWeight="SemiBold" Margin="0,2,0,0" />
                </StackPanel>
                <DataGrid ItemsSource="{Binding DxAssist.RecentDecodes}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          PreviewMouseRightButtonDown="StationGrid_PreviewMouseRightButtonDown">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="CALL NOW" Command="{Binding PlacementTarget.DataContext.CallNowCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                            <MenuItem Header="Suppress indefinitely" Command="{Binding PlacementTarget.DataContext.PermanentlySuppressCallsignCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                            <MenuItem Header="Release suppression" Command="{Binding PlacementTarget.DataContext.ReleaseSuppressionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource SemanticStationRowStyle}">
                            <Setter Property="ToolTip" Value="{Binding ParserInterpretation}" />
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Rank" Binding="{Binding RankText}" Width="48" />
                        <DataGridTemplateColumn Header="Call" CellTemplate="{StaticResource WorkedCallsignContactableCallTemplate}" SortMemberPath="ContactableCall" Width="88" />
                        <DataGridTextColumn Header="Country" Binding="{Binding CountryDisplay}" Width="135" />
                        <DataGridTextColumn Header="Grid" Binding="{Binding Grid}" Width="62" />
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="52" />
                        <DataGridTextColumn Header="Age" Binding="{Binding AgeText}" Width="54" />
                        <DataGridTextColumn Header="SNR" Binding="{Binding Snr}" Width="46" />
                        <DataGridTextColumn Header="JTDX Row" Binding="{Binding JtdxRow}" Width="70" />
                        <DataGridTextColumn Header="Wanted Reason" Binding="{Binding WantedReasonDisplay}" Width="170" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StationStatusDisplay}" Width="110" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Expander Grid.Row="2" Grid.ColumnSpan="2" Header="Technical status and controls" Margin="0,12,0,0" IsExpanded="False" FontWeight="SemiBold">
            <Border Style="{StaticResource PanelBorder}" Margin="0,7,0,0" Background="{StaticResource AppSurfaceMuted}">
                <UniformGrid Columns="5">
                    <StackPanel Margin="0,0,18,0">
                        <TextBlock Text="UDP CONNECTION" Style="{StaticResource SectionEyebrow}" />
                        <TextBlock Text="{Binding Dashboard.UdpStatus}" TextWrapping="Wrap" Margin="0,5,0,0" />
                        <WrapPanel Margin="0,8,0,0">
                            <Button Content="Start UDP" Command="{Binding StartUdpCommand}" MinWidth="74" />
                            <Button Content="Stop UDP" Command="{Binding StopUdpCommand}" MinWidth="74" />
                        </WrapPanel>
                    </StackPanel>
                    <StackPanel Margin="0,0,18,0">
                        <TextBlock Text="AUTOMATION" Style="{StaticResource SectionEyebrow}" />
                        <TextBlock Text="{Binding Dashboard.AutoResumeStatus}" TextWrapping="Wrap" Margin="0,5,0,0" />
                    </StackPanel>
                    <StackPanel Margin="0,0,18,0">
                        <TextBlock Text="TRANSMIT SAFETY" Style="{StaticResource SectionEyebrow}" />
                        <TextBlock Text="{Binding Dashboard.PixelState}" TextWrapping="Wrap" Margin="0,5,0,0" />
                    </StackPanel>
                    <StackPanel Margin="0,0,18,0">
                        <TextBlock Text="OBSERVED BEST" Style="{StaticResource SectionEyebrow}" />
                        <TextBlock Text="{Binding Dashboard.BestTarget}" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,5,0,0" />
                        <TextBlock Text="{Binding Dashboard.BestReason}" TextWrapping="Wrap" Foreground="{StaticResource AppTextMuted}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="RECOVERY" Style="{StaticResource SectionEyebrow}" />
                        <TextBlock Text="{Binding Dashboard.HuntState}" TextWrapping="Wrap" Margin="0,5,0,0" />
                        <TextBlock Text="{Binding Dashboard.ResumeCount, StringFormat=Automatic recoveries: {0}}" Foreground="{StaticResource AppTextMuted}" />
                    </StackPanel>
                </UniformGrid>
            </Border>
        </Expander>
    </Grid>
</UserControl>
